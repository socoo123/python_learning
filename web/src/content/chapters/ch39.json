{
  "id": "ch39",
  "num": "39",
  "title": "动态规划(Dynamic Programming)",
  "runMode": "pyodide",
  "tutorialMd": "# Ch39 · 动态规划(Dynamic Programming)\n\n> **预计**:1-2 天 ｜ **前置**:Ch34(刷题利器)、Ch36(前缀和)、Ch38(递归/树) ｜ **M6 核心**\n> **目标**:拿下面试最高频的算法主题——动态规划。掌握**两把写法**(自顶向下记忆化 / 自底向上填表)+ **五大原型题**(一维递推、完全背包、LIS、LCS、编辑距离)。\n\n> 📐 **本教程的契约**:§39.2–§39.6 对应作业 5 个函数,**一节一题**。纯 stdlib(`functools.lru_cache`),不 import 外部库。\n\n---\n\n## 🗺️ 作业↔知识点对应表\n\n| 函数(作业) | 对应小节 | 核心知识点 | 难度 |\n|------|----------|-----------|------|\n| `climb_stairs` | §39.2 | 一维递推 / 记忆化递归 / `@lru_cache` | 🟡 |\n| `coin_change` | §39.3 | 完全背包(求最少硬币)/ 自底向上填表 / 哨兵 | 🟡 |\n| `length_of_lis` | §39.4 | 一维 DP(`dp[i]`=以 i 结尾)/ O(n²) | 🟡 |\n| `longest_common_subsequence` | §39.5 | 二维 DP(双串)/ 边界填 0 | 🟡 |\n| `min_distance` | §39.6 | 编辑距离(Hard)/ 二维 DP / 增删改三选一 | 🔴 |\n\n---\n\n## ⏱️ 学习路径:费曼五步(约 90 分钟)\n\n① 预览猜 → ② 写 assignment → ③ pytest 红绿 → ④ 费曼 → ⑤ 存闪卡。\n\n---\n\n## §39.1 DP 是什么 + 什么时候用 🟡\n\n**动态规划**的本质一句话:**把大问题拆成子问题,记住每个子问题的答案,避免重复计算**。\n\n它和分治(如归并排序)的区别:分治的子问题**互不重叠**,DP 的子问题**大量重叠**。斐波那契 `f(5)` 会反复算 `f(3)`、`f(2)`——重叠子问题正是 DP 的舞台。\n\n### 识别一道题是不是 DP(三个信号)\n\n| 信号 | 说明 | 本章对应 |\n|------|------|----------|\n| **求最值**(最大/最小/最长/最短/最少) | \"最少硬币\"\"最长子序列\"\"最少操作数\" | §39.3/§39.4/§39.6 |\n| **求方案数**(有多少种走法/组合) | \"爬楼梯有几种走法\" | §39.2 |\n| **求可行性**(能不能凑出/到达) | \"能不能凑成 target\" | (变体) |\n\n外加两个理论前提:**最优子结构**(大问题的最优解由子问题最优解拼成)+ **无后效性**(未来的决策只看当前状态,不关心怎么走到这的)。满足这两条,DP 基本跑不掉。\n\n### 两把写法(背下来)\n\n| 写法 | 思路 | Python 利器 | Java 怎么写 |\n|------|------|------------|-------------|\n| **自顶向下(记忆化搜索)** | 从大问题出发递归,顺手把算过的子问题缓存 | `@lru_cache(None)` 一行搞定 | 手写 `int[] memo = new int[n]; Arrays.fill(memo, -1);` |\n| **自底向上(迭代填表)** | 从最小子问题开始,一格一格填 DP 表 | `dp = [0]*(n+1)` | 同样 `int[] dp = new int[n+1];` |\n\n**选择原则**:能写出递推关系的优先自底向上(常数小、无栈溢出);递归关系天然直观的(爬楼梯、树形 DP)用 `@lru_cache` 爽一把。\n\n> 🟡 **Java 对比**:Java 也有记忆化,但要你**自己维护 memo 数组**、判\"是否算过\"、传下标。Python 的 `@lru_cache` 是**装饰器**,自动用一个 dict 缓存\"参数 → 返回值\",你只管写干净的递归。这是 Python 刷 DP 题最大的爽点。\n\n> ✅ **本章贯穿口诀**:**状态定义 + 转移方程 + 边界**。每道题先问三连:① `dp[i]`(或 `dp[i][j]`)代表什么?② 它从哪些状态转移来?③ 最小的、可以直接回答的边界值是几?\n\n---\n\n## ① 预览猜\n\n1. 爬楼梯:每次能爬 1 或 2 步,爬到第 5 阶有几种走法?(提示:f(n)=f(n-1)+f(n-2),斐波那契)\n2. 为什么裸递归算斐波那契会慢到爆炸?子问题重叠长什么样?\n3. 「最少硬币数」为什么不能贪心?(举例:面额 `[1,3,4]` 凑 6,贪心选 4+1+1=3 枚,但最优是 3+3=2 枚)\n4. 「最长递增子序列」和「最长公共子序列」差在哪?(前者一串数自己跟自己比,后者两串字符互相比)\n5. 编辑距离的三种操作(增/删/改)怎么对应到二维表的三个邻居?\n\n---\n\n## §39.2 爬楼梯:climb_stairs(LC70)🟡\n\n**题面**:你每次能爬 1 或 2 步,爬到第 `n` 阶共有多少种不同走法?\n\n### 为什么这么做\n\n关键洞察——**只看最后一步**:到达第 `n` 阶,你的最后一步要么从第 `n-1` 阶爬 1 步、要么从第 `n-2` 阶爬 2 步(没有别的可能)。所以:\n\n```\nf(n) = f(n-1) + f(n-2)\nf(1) = 1   # 只能 1\nf(2) = 2   # 1+1 或 2\n```\n\n这就是**斐波那契数列**!\n\n为什么裸递归 `f(n)=f(n-1)+f(n-2)` 是 O(2ⁿ) 地狱?因为 `f(5)` 算 `f(4)+f(3)`,`f(4)` 又算 `f(3)+f(2)`——`f(3)` 被算了两次,越往下重复越多,呈指数爆炸。**子问题大量重叠** = DP 信号。\n\n### 实现:@lru_cache 记忆化递归\n\n```python\nfrom functools import lru_cache\n\ndef climb_stairs(n: int) -> int:\n    @lru_cache(None)\n    def f(k: int) -> int:\n        if k <= 2:        # 边界: f(1)=1, f(2)=2\n            return k\n        return f(k - 1) + f(k - 2)   # 转移方程\n    return f(n)\n```\n\n**亮点**:递归函数 `f` 加 `@lru_cache(None)` 后,`f(3)` 第一次算完会被缓存,之后再调直接返回——把 O(2ⁿ) 砍成 O(n)。你写的代码和裸递归**长得一模一样**,只多一行装饰器。Java 想达到同样效果,得自己开 `int[] memo`、判 `-1`、填值,啰嗦得多。\n\n> 为什么在函数内部再定义 `f` 而不是直接装饰 `climb_stairs`?——把缓存和公开 API 解耦,避免缓存被多次调用累积(可选习惯,把 `@lru_cache` 直接加到 `climb_stairs` 也对)。\n\n### 自底向上写法(对比理解)\n\n```python\ndef climb_stairs(n: int) -> int:\n    if n <= 2:\n        return n\n    dp = [0] * (n + 1)\n    dp[1], dp[2] = 1, 2\n    for i in range(3, n + 1):\n        dp[i] = dp[i - 1] + dp[i - 2]\n    return dp[n]\n```\n\n进一步还能优化空间到 O(1):只留两个变量滚动(`a, b = b, a + b`),但 DP 表更直观,面试先讲表。\n\n> 🟢 **Java 秒懂**:`dp` 数组那版几乎是 1:1 翻译(Java `int[] dp = new int[n+1];`)。\n> 🟡 **差异**:`@lru_cache` 是 Python 独有的优雅;Java 记忆化要手写 memo。\n> ⚠️ **坑**:`n<=2` 直接返回 `n`(即 `f(1)=1, f(2)=2`),别写成 `f(0)=0, f(1)=1` 再算——能对,但多绕一步容易写错边界。\n\n### 复杂度\n\n- 时间 O(n):每个子问题 `f(k)` 只算一次。\n- 空间 O(n):递归栈深度 + lru_cache 缓存 n 项;自底向上空间 O(n),滚动优化 O(1)。\n\n---\n\n## §39.3 零钱兑换:coin_change(LC322)🟡\n\n**题面**:给定硬币面额 `coins` 和金额 `amount`,凑成该金额的**最少硬币数**;凑不出返回 `-1`。每种硬币**无限个**(完全背包)。\n\n### 为什么这么做 —— 为什么不能贪心\n\n直觉是\"每次挑最大面额尽量多拿\"(贪心)。但贪心**会出错**:\n\n```\ncoins=[1,3,4], amount=6\n贪心: 4 + 1 + 1 = 3 枚   ← 错!\n最优: 3 + 3     = 2 枚   ✓\n```\n\n贪心只顾眼前,会错过\"两个 3 比 4+1+1 更优\"的组合。**必须穷举所有子问题**——这就是 DP。\n\n### 状态定义 + 转移\n\n```\ndp[i] = 凑成金额 i 所需的最少硬币数\n转移: dp[i] = min( dp[i - coin] + 1 )   for coin in coins, if coin <= i\n        ↑ 用一枚面额 coin 后,剩下的 i-coin 已经是最优,再 +1(这枚)\n边界: dp[0] = 0     (金额 0 不需要硬币)\n```\n\n**哨兵技巧**:无法凑出的金额怎么表示?把 `dp` 初始化成 `amount + 1`(一个\"不可能的大值\")——因为最优答案最多用 `amount` 个 1 元硬币,真实答案**绝不可能超过 `amount`**。所以 `amount+1` 就是\"无穷大\"的替身。最后看 `dp[amount]`,若仍 `> amount` 说明不可达,返回 `-1`。\n\n> 这是 DP 题里**最常见的哨兵模式**:用\"理论上界 +1\"代替 `float('inf')`,避免类型混乱,也方便 `min` 运算。\n\n### 实现\n\n```python\ndef coin_change(coins: list[int], amount: int) -> int:\n    MAX = amount + 1                      # 哨兵 = 不可能的大值\n    dp = [MAX] * (amount + 1)\n    dp[0] = 0                             # 边界\n    for i in range(1, amount + 1):\n        for c in coins:\n            if c <= i and dp[i - c] + 1 < dp[i]:\n                dp[i] = dp[i - c] + 1\n    return dp[amount] if dp[amount] != MAX else -1\n```\n\n**走一遍 `[1,2,5], amount=11`**:`dp[0]=0, dp[1]=1, dp[2]=1, dp[5]=1, dp[6]=2(5+1), ... dp[11]=3(10+1 / 6+5 / 9+2 的最优是 5+5+1)`。\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:`int[] dp = new int[amount+1]; Arrays.fill(dp, amount+1); dp[0]=0;` 双层 for 一模一样。\n> ⚠️ **坑**:\n> 1. 忘了 `amount=0` 返回 0(题目明确 0 元要 0 枚)。\n> 2. 哨兵写成 `float('inf')` 再 `+1` 会得到 `inf`,逻辑没错但最后比较 `> amount` 才是对的判据;新手容易直接 `return dp[amount]` 漏掉 `-1` 转换。\n> 3. 内层循环 `if c <= i` 必须判,否则 `dp[i-c]` 越界。\n\n### 复杂度\n\n- 时间 O(amount × len(coins)):两层循环。\n- 空间 O(amount):一维 dp 表。\n\n---\n\n## §39.4 最长递增子序列:length_of_lis(LC300)🟡\n\n**题面**:给定整数数组,返回**严格递增子序列**的最大长度(子序列**不要求连续**,但要保序)。\n\n### 为什么这么做\n\n注意两个限定:**严格递增**(`nums[j] < nums[i]`,不能等于)+ **不连续**(可跳着选,区别于\"子串/子数组\"必须连续)。\n\n经典 **一维 DP** 定义——以\"位置 i 结尾\"作状态:\n\n```\ndp[i] = 以 nums[i] 结尾的 LIS 长度\n转移: dp[i] = max( dp[j] + 1 )   for all j < i 且 nums[j] < nums[i]\n边界: dp[i] = 1   (至少包含 nums[i] 自己)\n答案: max(dp)   (LIS 可能以任意位置结尾,取全局最大)\n```\n\n**走一遍** `[10,9,2,5,3,7,101,18]`:\n\n| i | nums[i] | 能接在谁后面 (j) | dp[i] |\n|---|---------|------------------|-------|\n| 0 | 10 | (无) | 1 |\n| 1 | 9 | (无,9<前面的10不行) | 1 |\n| 2 | 2 | (无) | 1 |\n| 3 | 5 | 2(2<5) | dp[2]+1=2 |\n| 4 | 3 | 2(2<3) | dp[2]+1=2 |\n| 5 | 7 | 2,5,3 | max(1,2,2)+1=3 |\n| 6 | 101 | 2,5,3,7,9,10 | 3+1=4 |\n| 7 | 18 | 2,5,3,7,9,10 | 3+1=4 |\n\n`max(dp)=4`,答案 4(LIS 如 `[2,3,7,101]` 或 `[2,5,7,101]`)。\n\n### 实现\n\n```python\ndef length_of_lis(nums: list[int]) -> int:\n    if not nums:\n        return 0\n    n = len(nums)\n    dp = [1] * n              # 边界:每个元素自身是长度 1 的 LIS\n    for i in range(n):\n        for j in range(i):\n            if nums[j] < nums[i] and dp[j] + 1 > dp[i]:\n                dp[i] = dp[j] + 1\n    return max(dp)\n```\n\n### 进阶:O(n log n) 的 patience sorting(bisect)\n\n面试官常追问\"能不能更快\"。**贪心 + 二分**思路:维护一个数组 `tails`,`tails[k]` = 长度为 k+1 的所有递增子序列的**最小末尾**。每来一个新数,用 `bisect_left` 找它该替换/追加的位置:\n\n```python\nimport bisect\ndef length_of_lis_fast(nums):\n    tails = []\n    for x in nums:\n        i = bisect.bisect_left(tails, x)   # 第一个 >= x 的位置\n        if i == len(tails):\n            tails.append(x)                 # x 比所有都大 -> 接长\n        else:\n            tails[i] = x                    # 否则替换,让末尾更小\n    return len(tails)\n```\n\n注意:这个 `tails` **不是真实的 LIS**(只是长度对),但能求出**长度**且 O(n log n)。本章作业用 O(n²) 版本即可,进阶版面试加分。\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:双层 for + `dp` 数组,完全 1:1。\n> 🟡 **进阶差异**:Java 的二分要自己写或用 `Arrays.binarySearch`(返回负数表示插入点,要 `-i-1` 转换);Python 直接 `bisect.bisect_left`,语义清晰。\n> ⚠️ **坑**:\n> 1. 答案是 `max(dp)` 不是 `dp[-1]`——LIS 不一定以最后一个元素结尾。\n> 2. 空数组返回 0(题面隐含),忘了判 `if not nums` 会 `max([])` 报错。\n> 3. **严格递增**用 `<` 不是 `<=`;若题改成\"非递减\"才用 `<=`,别混。\n\n### 复杂度\n\n- DP 版:时间 O(n²),空间 O(n)。\n- 进阶版:时间 O(n log n),空间 O(n)。\n\n---\n\n## §39.5 最长公共子序列:longest_common_subsequence(LC1143)🟡\n\n**题面**:两个字符串 `text1`、`text2` 的**最长公共子序列**长度(可不连续,但保序)。\n\n### 为什么这么做\n\n两个串互相比——升到**二维状态**。经典定义:\n\n```\ndp[i][j] = text1 的前 i 个字符 与 text2 的前 j 个字符 的 LCS 长度\n```\n\n转移看最后那对字符 `text1[i-1]` 和 `text2[j-1]`(用 i-1 是因为字符串下标从 0,而 dp 从 1 开始留出\"空前缀\"边界):\n\n```\n若 text1[i-1] == text2[j-1]:   # 这对字符相等,可以配对收尾\n    dp[i][j] = dp[i-1][j-1] + 1\n否则:                          # 不等,各退一格取较大\n    dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n边界: dp[0][*] = dp[*][0] = 0   # 空串和任何串的 LCS = 0\n答案: dp[len1][len2]\n```\n\n**直观理解转移**:两字符相等,这俩就是 LCS 的一部分,前面的 LCS 长度 +1;不等,那这俩至少有一个**不进** LCS,分别尝试扔掉一个取较大。\n\n**走一遍** `\"abcde\"` vs `\"ace\"`(dp 表 4×6,行是 abcde、列是 ace):\n\n```\n      \"\"  a   c   e\n  \"\"   0  0   0   0\n  a    0  1   1   1     # a==a -> dp[0][0]+1=1\n  b    0  1   1   1     # b不配 -> max(上,左)\n  c    0  1   2   2     # c==c -> dp[1][1]+1=2\n  d    0  1   2   2     # 不配\n  e    0  1   2   3     # e==e -> dp[4][2]+1=3\n```\n\n`dp[5][3]=3`,答案 3(即 `\"ace\"`)。\n\n### 实现\n\n```python\ndef longest_common_subsequence(text1: str, text2: str) -> int:\n    len1, len2 = len(text1), len(text2)\n    # dp 表开 (len1+1) x (len2+1),第 0 行/列全 0(空前缀边界)\n    dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]\n    for i in range(1, len1 + 1):\n        for j in range(1, len2 + 1):\n            if text1[i - 1] == text2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1] + 1\n            else:\n                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])\n    return dp[len1][len2]\n```\n\n### Java 对比 + 二维表的常见写法\n\n> 🟢 **Java 秒懂**:`int[][] dp = new int[len1+1][len2+1];` 几乎逐字翻译。\n> 🔴 **Python 特有**:**二维 list 的初始化**是个经典坑——\n> ```python\n> dp = [[0] * (len2 + 1)] * (len1 + 1)   # ❌ 错!所有行是同一个对象\n> dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]   # ✓ 每行独立\n> ```\n> `[x]*n` 对**不可变**元素(0)是安全的,但 `[[..]]*n` 会让 n 行指向**同一行**,改一个全变。**必须用列表推导式**。这是 Java 老手写 Python 二维表的第一大坑(Java 的 `new int[][]` 天然每行独立,没有这问题)。\n> ⚠️ **其他坑**:`i-1`/`j-1` 别越界(所以从 1 开始循环);别把 `dp[i-1][j-1]+1` 写成 `dp[i][j]+1`(自引用)。\n\n### 复杂度\n\n- 时间 O(len1 × len2):填满整张表。\n- 空间 O(len1 × len2);滚动数组可优化到 O(min(len1,len2))。\n\n---\n\n## §39.6 编辑距离:min_distance(LC72 Hard)🔴\n\n**题面**:把 `word1` 变成 `word2`,最少需要几次**单字符操作**?操作有三种:**插入 / 删除 / 替换** 一个字符。\n\n### 为什么这么做 —— LCS 的升级版\n\n这是二维 DP 的天花板题之一。和 LCS 几乎同构,但相等时要看\"不操作\",不等时**多了替换**。状态:\n\n```\ndp[i][j] = word1 前 i 个字符 变成 word2 前 j 个字符 的最少操作数\n```\n\n转移:\n\n```\n若 word1[i-1] == word2[j-1]:   # 这对字符已相等,白嫖,不操作\n    dp[i][j] = dp[i-1][j-1]\n否则:                          # 不等,三选一取最小 +1\n    dp[i][j] = 1 + min(\n        dp[i-1][j],      # 删除 word1[i-1]    (word1 退一格)\n        dp[i][j-1],      # 插入(=在 word1 加 word2[j-1],word2 退一格)\n        dp[i-1][j-1],    # 替换 word1[i-1] 成 word2[j-1]\n    )\n边界:\n    dp[i][0] = i   # 把 word1 前 i 个全删了变空串\n    dp[0][j] = j   # 从空串插入 j 个字符变 word2 前 j 个\n```\n\n**三种操作怎么对应三个邻居**(画表秒懂):\n- `dp[i-1][j]` 是**上方** → word1 多消耗一个字符(删掉它)。\n- `dp[i][j-1] 是**左方** → word2 多匹配一个(在 word1 插入它)。\n- `dp[i-1][j-1]` 是**左上** → 两个指针都前进(替换,或相等时白嫖)。\n\n**走一遍** `\"horse\"` → `\"ros\"`(简略):最终 `dp[5][3]=3`(horse→rorse→rose→ros)。\n\n### 实现\n\n```python\ndef min_distance(word1: str, word2: str) -> int:\n    len1, len2 = len(word1), len(word2)\n    dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]\n    # 边界:空串\n    for i in range(len1 + 1):\n        dp[i][0] = i\n    for j in range(len2 + 1):\n        dp[0][j] = j\n    for i in range(1, len1 + 1):\n        for j in range(1, len2 + 1):\n            if word1[i - 1] == word2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1]   # 白嫖\n            else:\n                dp[i][j] = 1 + min(\n                    dp[i - 1][j],      # 删\n                    dp[i][j - 1],      # 插\n                    dp[i - 1][j - 1],  # 替\n                )\n    return dp[len1][len2]\n```\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:二维 `int[][]`,三层填表逻辑完全一致。Hard 的\"难\"在**想到状态定义**,不在语言。\n> 🟡 **差异**:同样是 `[[0]*w]*h` 的初始化坑(见 §39.5),用列表推导式。\n> ⚠️ **坑**:\n> 1. **边界别漏**:空串情况 `dp[i][0]=i`、`dp[0][j]=j` 必须显式填,否则默认 0 会把\"全删/全插\"算成 0 次。\n> 2. 字符相等时是 `dp[i-1][j-1]`(**不加 1**,白嫖),不等时才 `+1`——两者别混。\n> 3. 别忘了 `min` 里**三个**分支,漏掉\"替换\"会退化成只能增删(那是另一道题:只有增删的编辑距离 = `len1+len2-2*LCS`)。\n\n### 复杂度\n\n- 时间 O(len1 × len2):填满整张表。\n- 空间 O(len1 × len2);滚动数组优化到 O(min(len1,len2))。\n\n---\n\n## §39.7 DP 通用方法论 + 原型速查(讲透不出题)\n\n### 解 DP 题的四步套路(背下来)\n\n1. **定义状态**:`dp[i]` / `dp[i][j]` 代表什么?(一句话,要\"无后效\")\n2. **写转移方程**:`dp[i]` 从哪些状态来?取 `max`/`min`/`+`?\n3. **定边界 + 初始值**:最小子问题直接给答案(如 `dp[0]=0`),不可达用哨兵。\n4. **确定计算顺序 + 答案位置**:自底向上从小填大;答案在 `dp[n]` 还是 `max(dp)`?\n\n### 五大原型速查表\n\n| 原型 | 代表题 | 状态 | 转移核心 |\n|------|--------|------|----------|\n| 一维递推(斐波那契型) | 爬楼梯 §39.2 | `dp[i]`=到 i 的方案数 | `dp[i]=dp[i-1]+dp[i-2]` |\n| 背包(完全/0-1) | 零钱兑换 §39.3 | `dp[i]`=凑 i 的最少/方案 | `dp[i]=min(dp[i-c]+1)` |\n| 一维区间(LIS 类) | LIS §39.4 | `dp[i]`=以 i 结尾 | `dp[i]=max(dp[j]+1)` |\n| 双串公共(LCS 类) | LCS §39.5 | `dp[i][j]`=前 i × 前 j | 相等 +1,否则 max(上,左) |\n| 双串变换(编辑距离类) | Edit §39.6 | `dp[i][j]`=i→j 的代价 | 相等不变,否则 min(增删改)+1 |\n\n---\n\n## §39.8 Java 老手常踩的坑 ⚠️\n\n1. **裸递归不记忆化**:斐波那契、爬楼梯写裸递归 → O(2ⁿ) 超时。要么 `@lru_cache`,要么填表。\n2. **二维表用 `[[0]*w]*h`**:`*` 复制是浅拷贝,所有行指向同一对象,改一个全变。**必须列表推导式** `[[0]*w for _ in range(h)]`(§39.5/§39.6 大坑)。\n3. **答案取错位置**:LIS 答案是 `max(dp)` 不是 `dp[-1]`(LIS 不一定以末尾结束);爬楼梯答案在 `dp[n]`。\n4. **哨兵漏判**:`coin_change` 不可达要返回 `-1`,别直接 `return dp[amount]`(可能是哨兵)。\n5. **边界不填**:编辑距离的 `dp[i][0]=i`、`dp[0][j]=j` 必须显式填,否则全算成 0。\n6. **严格 vs 非严格搞混**:LIS 用 `<`(严格),题改\"非递减\"才用 `<=`。\n7. **三操作漏分支**:编辑距离 `min(删,插,替)` 三个都要,漏\"替换\"答案错。\n8. **`@lru_cache` 加在错的函数上**:可变参数(list)不能 hash → 不能缓存;要缓存就把 list 转 tuple,或缓存下标版。\n\n---\n\n## 📝 本章作业\n\n| 任务 | 知识点 | 难度 |\n|------|--------|------|\n| `climb_stairs` | 一维递推 + `@lru_cache` | 🟡 |\n| `coin_change` | 完全背包求最小 + 哨兵 | 🟡 |\n| `length_of_lis` | 一维 DP O(n²) | 🟡 |\n| `longest_common_subsequence` | 二维 DP 双串 | 🟡 |\n| `min_distance` | 编辑距离 Hard | 🔴 |\n\n```bash\nuv run pytest 06_leetcode/ch39/test_ch39_assignment.py -v\n```\n\n全绿 = 你掌握了 Ch39。\n\n---\n\n## ✅ 自测\n\n- [ ] 能背出 DP 四步套路:状态 → 转移 → 边界 → 顺序/答案位置\n- [ ] 会用 `@lru_cache` 写记忆化,知道它怎么把 O(2ⁿ) 砍成 O(n)\n- [ ] 知道 coin_change 为什么不能贪心,会写哨兵判 `-1`\n- [ ] LIS 的 `dp[i]` 是\"以 i 结尾\"不是\"前 i 个\",答案是 `max(dp)`\n- [ ] 二维表会用 `[[0]*w for _ in range(h)]` 避免 `*h` 浅拷贝坑\n- [ ] 编辑距离三操作能对到表的\"上/左/左上\"三个邻居\n- [ ] 5 个作业全绿\n\n## 🎓 费曼挑战\n\n1. 「爬楼梯裸递归为什么会慢到爆炸?`@lru_cache` 怎么救?Java 里你得怎么手动做这件事?」— 重读 §39.2\n2. 「零钱兑换为什么不能贪心?`amount+1` 这个哨兵为什么不影响答案正确性?」— 重读 §39.3\n3. 「LIS 的 `dp[i]` 为什么定义成'以 i 结尾'而不是'前 i 个'?两种定义答案位置分别在哪?」— 重读 §39.4\n4. 「LCS 和编辑距离的状态转移几乎一样,区别在哪一格?为什么编辑距离相等时不 +1?」— 重读 §39.5 + §39.6\n5. 「写 Python 二维 DP 表,`[[0]*w]*h` 会出什么 bug?为什么?」— 重读 §39.5\n\n## 🧠 记忆闪卡 → [`review.md`](./review.md)\n\n---\n\n## ⏭️ 下一步:Ch40 回溯 / 贪心 + 综合\n\nDP 解决\"求最值/方案数且子问题重叠\"。接下来 **回溯**(DFS 枚举所有可能:全排列、组合、N 皇后)+ **贪心**(每步局部最优 = 全局最优的特定题型)。这两类和 DP 互补:回溯是\"穷举所有方案\",贪心是\"不回头地选\",DP 是\"重叠子问题求最优\"。凑齐 M6 工具箱。\n",
  "assignment": "\"\"\"\nCh39 作业:动态规划(Dynamic Programming)。\n\nDP 是面试核心。核心思想:**把大问题拆成子问题,记住子问题的答案避免重复计算**。\n两种写法:\n  - 自顶向下(记忆化搜索):写递归 + 用缓存记住已算的子问题。Python 用 @lru_cache\n    写得极优雅;Java 要手写数组/HashMap 当缓存。\n  - 自底向上(迭代 DP):从最小子问题开始填表,用 list 当 DP 表。\n\n本章 5 道经典题,覆盖 DP 三大原型:\n  1. 一维递推(爬楼梯:斐波那契型)\n  2. 背包/凑硬币(完全背包求最少硬币)\n  3. 子序列(LIS、LCS)\n  4. 区间/双串(编辑距离)\n\n纯 stdlib。在每处 TODO 写实现,然后:\n\n    uv run pytest 06_leetcode/ch39/test_ch39_assignment.py -v\n\n全绿 = 你掌握了 Ch39。\n\"\"\"\nfrom functools import lru_cache\n\n\n# ========== §39.2 爬楼梯:climb_stairs ==========\n\n\ndef climb_stairs(n: int) -> int:\n    \"\"\"\n    【爬楼梯 LC70 · §39.2】每次能爬 1 或 2 步,爬到第 n 阶有几种不同走法?\n\n    示例:\n        climb_stairs(2) -> 2   # 1+1, 2\n        climb_stairs(3) -> 3   # 1+1+1, 1+2, 2+1\n        climb_stairs(5) -> 8\n        climb_stairs(1) -> 1\n\n    思路(@lru_cache 记忆化递归,f(n)=f(n-1)+f(n-2)):\n        到达第 n 阶,最后一步要么从 n-1 爬 1 步、要么从 n-2 爬 2 步,\n        所以走法 = f(n-1) + f(n-2)。这就是斐波那契,f(1)=1, f(2)=2。\n        用 @lru_cache(None) 自动缓存子问题答案,避免指数级重复计算。\n    \"\"\"\n    # TODO: 按上方「思路」实现(可用 @lru_cache 记忆化:f(n)=f(n-1)+f(n-2),f(1)=1,f(2)=2)\n    ...\n\n\n# ========== §39.3 零钱兑换:coin_change ==========\n\n\ndef coin_change(coins: list[int], amount: int) -> int:\n    \"\"\"\n    【零钱兑换 LC322 · §39.3】用最少的硬币凑成 amount,凑不出返回 -1。每种硬币无限个。\n\n    示例:\n        coin_change([1,2,5], 11)  -> 3   # 5+5+1\n        coin_change([2], 3)       -> -1\n        coin_change([1], 0)       -> 0\n        coin_change([1], 2)       -> 2\n\n    思路(自底向上 DP,完全背包求最小):\n        dp[i] = 凑成金额 i 所需的最少硬币数。\n        转移:dp[i] = min(dp[i-coin] + 1)  for coin in coins if coin <= i\n        初始:dp[0] = 0(金额 0 不要硬币),其余初始化成 amount+1(表示\"无穷大/不可达\")。\n        最后 dp[amount] > amount 说明凑不出,返回 -1。\n        用 amount+1 当哨兵是因为最多用 amount 个 1 元硬币,真实答案不可能超过它。\n    \"\"\"\n    # TODO: 按上方「思路」实现(自底向上 DP,dp[i]=min(dp[i-c]+1),哨兵 amount+1)\n    ...\n\n\n# ========== §39.4 最长递增子序列:length_of_lis ==========\n\n\ndef length_of_lis(nums: list[int]) -> int:\n    \"\"\"\n    【最长递增子序列 LC300 · §39.4】返回严格递增子序列的最大长度(不要求连续)。\n\n    示例:\n        length_of_lis([10,9,2,5,3,7,101,18]) -> 4   # [2,3,7,101] 或 [2,5,7,101]\n        length_of_lis([0,1,0,3,2,3])         -> 4   # [0,1,2,3]\n        length_of_lis([7,7,7,7])             -> 1\n        length_of_lis([])                    -> 0\n\n    思路(O(n^2) DP):\n        dp[i] = 以 nums[i] 结尾的 LIS 长度。\n        对所有 j<i 且 nums[j]<nums[i]:dp[i] = max(dp[i], dp[j]+1)。\n        全程初始化 dp[i]=1(至少包含自己)。答案 = max(dp)。\n        空数组返回 0。\n    \"\"\"\n    # TODO: 按上方「思路」实现(dp[i]=以 nums[i] 结尾的 LIS,答案 max(dp);空数组 0)\n    ...\n\n\n# ========== §39.5 最长公共子序列:longest_common_subsequence ==========\n\n\ndef longest_common_subsequence(text1: str, text2: str) -> int:\n    \"\"\"\n    【最长公共子序列 LC1143 · §39.5】两个字符串的最长公共子序列长度(可不连续)。\n\n    示例:\n        longest_common_subsequence(\"abcde\", \"ace\") -> 3   # \"ace\"\n        longest_common_subsequence(\"abc\", \"abc\")   -> 3\n        longest_common_subsequence(\"abc\", \"def\")   -> 0\n\n    思路(二维 DP):\n        dp[i][j] = text1 前 i 个字符 与 text2 前 j 个字符 的 LCS 长度。\n        - 若 text1[i-1]==text2[j-1]:dp[i][j] = dp[i-1][j-1] + 1  (这对字符配对)\n        - 否则:dp[i][j] = max(dp[i-1][j], dp[i][j-1])  (扔掉一个字符取较大)\n        初始:dp[0][*]=dp[*][0]=0(空串与任何串的 LCS=0)。\n        答案 = dp[len1][len2]。\n        为了让 i-1/j-1 不越界,dp 表开 (len1+1)x(len2+1),第 0 行/列全 0。\n    \"\"\"\n    # TODO: 按上方「思路」实现(二维 DP;相等 +1,否则 max(上,左))\n    ...\n\n\n# ========== §39.6 编辑距离:min_distance ==========\n\n\ndef min_distance(word1: str, word2: str) -> int:\n    \"\"\"\n    【编辑距离 LC72 Hard · §39.6】把 word1 变成 word2 最少需要几次操作\n    (插入 / 删除 / 替换 一个字符)。\n\n    示例:\n        min_distance(\"horse\", \"ros\")     -> 3   # horse->rorse->rose->ros\n        min_distance(\"intention\", \"execution\") -> 5\n        min_distance(\"\", \"\")             -> 0\n        min_distance(\"a\", \"\")            -> 1   # 删 a\n        min_distance(\"\", \"a\")            -> 1   # 插 a\n\n    思路(二维 DP,LCS 的升级版——多了\"替换\"操作):\n        dp[i][j] = word1 前 i 个字符 变成 word2 前 j 个字符 的最少操作数。\n        - 若 word1[i-1]==word2[j-1]:dp[i][j] = dp[i-1][j-1]  (这对字符已相等,不操作)\n        - 否则取三种操作的最小值 +1:\n            dp[i-1][j]   + 1   # 删除 word1[i-1]\n            dp[i][j-1]   + 1   # 在 word1 插入一个字符(等价于删除 word2[j-1])\n            dp[i-1][j-1] + 1   # 替换 word1[i-1] 成 word2[j-1]\n        初始:\n            dp[i][0] = i  (把 word1 前 i 个字符全删了变成空串)\n            dp[0][j] = j  (从空串插入 j 个字符变成 word2 前 j 个)\n    \"\"\"\n    # TODO: 按上方「思路」实现(二维 DP;相等不+1,否则 1+min(删/插/替);边界 dp[i][0]=i,dp[0][j]=j)\n    ...\n\n\n# ---------------------------------------------------------------------\nif __name__ == \"__main__\":\n    print(\"climb_stairs(5) =\", climb_stairs(5))  # 8\n    print(\"coin_change([1,2,5], 11) =\", coin_change([1, 2, 5], 11))  # 3\n    print(\"length_of_lis([10,9,2,5,3,7,101,18]) =\", length_of_lis([10, 9, 2, 5, 3, 7, 101, 18]))  # 4\n    print(\"longest_common_subsequence('abcde','ace') =\", longest_common_subsequence(\"abcde\", \"ace\"))  # 3\n    print(\"min_distance('horse','ros') =\", min_distance(\"horse\", \"ros\"))  # 3\n",
  "testName": "ch39_assignment",
  "testSource": "\"\"\"\nCh39 作业测试(动态规划)。运行: uv run pytest 06_leetcode/ch39/test_ch39_assignment.py -v\n\"\"\"\nimport pytest\n\nfrom ch39_assignment import (\n    climb_stairs,\n    coin_change,\n    length_of_lis,\n    longest_common_subsequence,\n    min_distance,\n)\n\n\n# ---------- climb_stairs (LC70) ----------\nclass TestClimbStairs:\n    def test_leetcode_example_2(self):\n        assert climb_stairs(2) == 2  # 1+1, 2\n\n    def test_leetcode_example_3(self):\n        assert climb_stairs(3) == 3  # 1+1+1, 1+2, 2+1\n\n    def test_base_one(self):\n        assert climb_stairs(1) == 1\n\n    def test_five(self):\n        assert climb_stairs(5) == 8  # 斐波那契: 1,2,3,5,8\n\n    def test_ten(self):\n        assert climb_stairs(10) == 89\n\n    def test_fibonacci_sequence(self):\n        # 验证 f(n)=f(n-1)+f(n-2) 成立\n        for n in range(3, 15):\n            assert climb_stairs(n) == climb_stairs(n - 1) + climb_stairs(n - 2)\n\n\n# ---------- coin_change (LC322) ----------\nclass TestCoinChange:\n    def test_leetcode_example(self):\n        assert coin_change([1, 2, 5], 11) == 3  # 5+5+1\n\n    def test_impossible(self):\n        assert coin_change([2], 3) == -1  # 凑不出 3\n\n    def test_zero_amount(self):\n        assert coin_change([1, 2, 5], 0) == 0\n\n    def test_single_coin_denom(self):\n        assert coin_change([1], 2) == 2  # 1+1\n\n    def test_single_coin_exact(self):\n        assert coin_change([5], 5) == 1\n\n    def test_need_multiple(self):\n        assert coin_change([1, 2, 5], 7) == 2  # 5+2\n\n    def test_large_with_one(self):\n        # 只能用 2 和 5,凑 13:5+5+2+1? 1 不在 -> 5+5+?3凑不出 -> 2*? 13 奇数不行\n        # 改成 [2,5] 凑 12 = 5+5+2 -> 3\n        assert coin_change([2, 5], 12) == 3\n\n    def test_impossible_all(self):\n        # 面额都 > amount 且 amount != 0 -> 凑不出\n        assert coin_change([5, 10], 3) == -1\n\n\n# ---------- length_of_lis (LC300) ----------\nclass TestLengthOfLis:\n    def test_leetcode_example(self):\n        assert length_of_lis([10, 9, 2, 5, 3, 7, 101, 18]) == 4\n\n    def test_second_example(self):\n        assert length_of_lis([0, 1, 0, 3, 2, 3]) == 4\n\n    def test_all_equal(self):\n        assert length_of_lis([7, 7, 7, 7]) == 1  # 严格递增,只能取 1 个\n\n    def test_empty(self):\n        assert length_of_lis([]) == 0\n\n    def test_single(self):\n        assert length_of_lis([42]) == 1\n\n    def test_strictly_increasing(self):\n        assert length_of_lis([1, 2, 3, 4, 5]) == 5\n\n    def test_strictly_decreasing(self):\n        assert length_of_lis([5, 4, 3, 2, 1]) == 1\n\n    def test_two_elements_increasing(self):\n        assert length_of_lis([1, 3]) == 2\n\n    def test_negative_numbers(self):\n        assert length_of_lis([-1, 0, -2, 3]) == 3  # -1,0,3\n\n\n# ---------- longest_common_subsequence (LC1143) ----------\nclass TestLongestCommonSubsequence:\n    def test_leetcode_example(self):\n        assert longest_common_subsequence(\"abcde\", \"ace\") == 3  # \"ace\"\n\n    def test_identical(self):\n        assert longest_common_subsequence(\"abc\", \"abc\") == 3\n\n    def test_no_common(self):\n        assert longest_common_subsequence(\"abc\", \"def\") == 0\n\n    def test_one_empty(self):\n        assert longest_common_subsequence(\"\", \"abc\") == 0\n        assert longest_common_subsequence(\"abc\", \"\") == 0\n\n    def test_both_empty(self):\n        assert longest_common_subsequence(\"\", \"\") == 0\n\n    def test_partial_overlap(self):\n        assert longest_common_subsequence(\"bsbininm\", \"jmjkbkjkv\") == 1  # \"b\"\n\n    def test_subsequence_at_end(self):\n        assert longest_common_subsequence(\"abcdef\", \"def\") == 3  # \"def\"\n\n    def test_repeated_chars(self):\n        assert longest_common_subsequence(\"aab\", \"ab\") == 2  # \"ab\"\n\n\n# ---------- min_distance (LC72) ----------\nclass TestMinDistance:\n    def test_leetcode_example(self):\n        assert min_distance(\"horse\", \"ros\") == 3\n\n    def test_leetcode_second_example(self):\n        assert min_distance(\"intention\", \"execution\") == 5\n\n    def test_both_empty(self):\n        assert min_distance(\"\", \"\") == 0\n\n    def test_first_empty(self):\n        assert min_distance(\"\", \"a\") == 1  # 插入 a\n\n    def test_second_empty(self):\n        assert min_distance(\"a\", \"\") == 1  # 删除 a\n\n    def test_identical(self):\n        assert min_distance(\"abc\", \"abc\") == 0  # 完全相同\n\n    def test_single_replace(self):\n        assert min_distance(\"a\", \"b\") == 1  # 替换\n\n    def test_single_insert(self):\n        assert min_distance(\"ab\", \"abc\") == 1  # 插入 c\n\n    def test_single_delete(self):\n        assert min_distance(\"abc\", \"ab\") == 1  # 删除 c\n",
  "reviewMd": "# Ch39 · 记忆闪卡 & 复习(动态规划)\n\n> Ultralearning 原则七·记忆留存。**先回忆,再翻答案**。连续 2 次秒答 → 退役 ✅。\n\n## 🔖 闪卡\n\n| # | 正面(问题) | 背面(答案) | 掌握 |\n|---|---|---|---|\n| 1 | DP 的本质一句话?和分治的区别? | 把大问题拆成子问题并**记住答案**避免重复计算。区别:分治子问题不重叠,DP 子问题大量重叠(斐波那契 f(3) 反复算) | ⬜ |\n| 2 | DP 两把写法?Python 利器? | 自顶向下(记忆化递归 + `@lru_cache`)vs 自底向上(迭代填 `dp` 表)。Java 记忆化要手写 `int[] memo` 判 -1,Python 一行装饰器 | ⬜ |\n| 3 | 识别 DP 的三个信号? | ① 求最值(最大/最少/最长)② 求方案数(多少种)③ 求可行性(能不能)。前提:最优子结构 + 无后效性 | ⬜ |\n| 4 | DP 四步套路? | ① 定义状态(dp[i]/dp[i][j] 代表什么)② 写转移方程 ③ 定边界+初始值(哨兵)④ 计算顺序+答案位置 | ⬜ |\n| 5 | 爬楼梯转移?为什么裸递归爆炸? | f(n)=f(n-1)+f(n-2),f(1)=1,f(2)=2(斐波那契)。裸递归子问题重叠呈 O(2ⁿ);`@lru_cache` 缓存后 O(n) | ⬜ |\n| 6 | coin_change 状态+转移? | dp[i]=凑成 i 的最少硬币数;dp[i]=min(dp[i-c]+1) for c in coins,c<=i;dp[0]=0 | ⬜ |\n| 7 | coin_change 哨兵为什么是 amount+1?不可达返回什么? | 最优答案最多用 amount 个 1 元硬币,绝不超过 amount;用 amount+1 当\"无穷大\"。最后 dp[amount]>amount 说明凑不出,返回 -1 | ⬜ |\n| 8 | coin_change 为什么不能贪心? | 反例 coins=[1,3,4] amount=6:贪心 4+1+1=3 枚,最优 3+3=2 枚。贪心只看眼前会错过组合 | ⬜ |\n| 9 | LIS 的 dp[i] 定义?答案在哪?复杂度? | dp[i]=**以 nums[i] 结尾**的 LIS 长度(不是\"前 i 个\")。dp[i]=max(dp[j]+1) for j<i,nums[j]<nums[i]。答案=max(dp)。O(n²),进阶 bisect O(nlogn) | ⬜ |\n| 10 | LIS 空数组/全相等的返回? | 空数组返回 0;全相等([7,7,7,7])返回 1(严格递增只能取 1 个) | ⬜ |\n| 11 | LCS 的状态+转移? | dp[i][j]=text1 前 i × text2 前 j 的 LCS 长。相等 dp[i-1][j-1]+1;否则 max(dp[i-1][j], dp[i][j-1])。边界 dp[0][*]=dp[*][0]=0 | ⬜ |\n| 12 | Python 写二维 DP 表的大坑? | `[[0]*w]*h` 是浅拷贝,所有行同一对象,改一个全变。必须 `[[0]*w for _ in range(h)]`(列表推导式每行独立)。Java 的 `new int[][]` 没这问题 | ⬜ |\n| 13 | 编辑距离三操作对应表的哪三个邻居? | 删除=上方 dp[i-1][j];插入=左方 dp[i][j-1];替换=左上 dp[i-1][j-1]。不等时 1+min(删,插,替) | ⬜ |\n| 14 | 编辑距离字符相等时为什么是 dp[i-1][j-1] 不加 1? | 字符相等\"白嫖\",这对字符已匹配,不需要操作,直接继承左上角的代价。不等时才 +1 | ⬜ |\n| 15 | 编辑距离边界 dp[i][0]/dp[0][j]?为什么不能漏? | dp[i][0]=i(word1 前 i 个全删变空),dp[0][j]=j(空串插 j 个)。漏填会默认 0,把\"全删/全插\"错算成 0 次 | ⬜ |\n| 16 | DP 空间优化通用技巧? | 滚动数组:二维 dp 只依赖\"上一行\",可压成一维/两个变量。爬楼梯 O(1)、LCS/编辑距离 O(min(m,n)) | ⬜ |\n| 17 | @lru_cache 能缓存什么参数? | 只能缓存**可 hash** 参数(tuple/str/int/数字)。list 不可 hash → 要先转 tuple,或改成缓存下标版 f(i) | ⬜ |\n\n## 🎓 费曼自检\n\n- [ ] 能背 DP 四步套路,并说清\"状态定义\"为什么是第一步最关键\n- [ ] 能说清\"`@lru_cache` 怎么把 O(2ⁿ) 砍成 O(n)\",以及 Java 里要怎么手动记忆化\n- [ ] 能说清\"coin_change 为什么不能贪心 + 哨兵 amount+1 的逻辑\"\n- [ ] 能说清\"LIS dp[i] 定义成'以 i 结尾'的代价——答案在 max(dp) 不在 dp[-1]\"\n- [ ] 能说清\"Python 二维表 `[[0]*w]*h` 的浅拷贝 bug + 列表推导式修法\"\n- [ ] 能说清\"编辑距离三操作 ↔ 表的上/左/左上 + 相等时白嫖不 +1\"\n\n## 📅 复习日程\n\n- [ ] +1 天　日期:________\n- [ ] +3 天　日期:________\n- [ ] +7 天　日期:________\n\n> 到期登记到根 [`REVIEW.md`](../../REVIEW.md)。\n",
  "interleaved": true,
  "sections": [
    {
      "id": "intro",
      "heading": "",
      "secNum": null,
      "body": "> **预计**:1-2 天 ｜ **前置**:Ch34(刷题利器)、Ch36(前缀和)、Ch38(递归/树) ｜ **M6 核心**\n> **目标**:拿下面试最高频的算法主题——动态规划。掌握**两把写法**(自顶向下记忆化 / 自底向上填表)+ **五大原型题**(一维递推、完全背包、LIS、LCS、编辑距离)。\n\n> 📐 **本教程的契约**:§39.2–§39.6 对应作业 5 个函数,**一节一题**。纯 stdlib(`functools.lru_cache`),不 import 外部库。\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-作业-知识点对应表",
      "heading": "🗺️ 作业↔知识点对应表",
      "secNum": null,
      "body": "| 函数(作业) | 对应小节 | 核心知识点 | 难度 |\n|------|----------|-----------|------|\n| `climb_stairs` | §39.2 | 一维递推 / 记忆化递归 / `@lru_cache` | 🟡 |\n| `coin_change` | §39.3 | 完全背包(求最少硬币)/ 自底向上填表 / 哨兵 | 🟡 |\n| `length_of_lis` | §39.4 | 一维 DP(`dp[i]`=以 i 结尾)/ O(n²) | 🟡 |\n| `longest_common_subsequence` | §39.5 | 二维 DP(双串)/ 边界填 0 | 🟡 |\n| `min_distance` | §39.6 | 编辑距离(Hard)/ 二维 DP / 增删改三选一 | 🔴 |\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-学习路径-费曼五步-约-90-分钟",
      "heading": "⏱️ 学习路径:费曼五步(约 90 分钟)",
      "secNum": null,
      "body": "① 预览猜 → ② 写 assignment → ③ pytest 红绿 → ④ 费曼 → ⑤ 存闪卡。\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-39.1",
      "heading": "§39.1 DP 是什么 + 什么时候用 🟡",
      "secNum": "39.1",
      "body": "**动态规划**的本质一句话:**把大问题拆成子问题,记住每个子问题的答案,避免重复计算**。\n\n它和分治(如归并排序)的区别:分治的子问题**互不重叠**,DP 的子问题**大量重叠**。斐波那契 `f(5)` 会反复算 `f(3)`、`f(2)`——重叠子问题正是 DP 的舞台。\n\n### 识别一道题是不是 DP(三个信号)\n\n| 信号 | 说明 | 本章对应 |\n|------|------|----------|\n| **求最值**(最大/最小/最长/最短/最少) | \"最少硬币\"\"最长子序列\"\"最少操作数\" | §39.3/§39.4/§39.6 |\n| **求方案数**(有多少种走法/组合) | \"爬楼梯有几种走法\" | §39.2 |\n| **求可行性**(能不能凑出/到达) | \"能不能凑成 target\" | (变体) |\n\n外加两个理论前提:**最优子结构**(大问题的最优解由子问题最优解拼成)+ **无后效性**(未来的决策只看当前状态,不关心怎么走到这的)。满足这两条,DP 基本跑不掉。\n\n### 两把写法(背下来)\n\n| 写法 | 思路 | Python 利器 | Java 怎么写 |\n|------|------|------------|-------------|\n| **自顶向下(记忆化搜索)** | 从大问题出发递归,顺手把算过的子问题缓存 | `@lru_cache(None)` 一行搞定 | 手写 `int[] memo = new int[n]; Arrays.fill(memo, -1);` |\n| **自底向上(迭代填表)** | 从最小子问题开始,一格一格填 DP 表 | `dp = [0]*(n+1)` | 同样 `int[] dp = new int[n+1];` |\n\n**选择原则**:能写出递推关系的优先自底向上(常数小、无栈溢出);递归关系天然直观的(爬楼梯、树形 DP)用 `@lru_cache` 爽一把。\n\n> 🟡 **Java 对比**:Java 也有记忆化,但要你**自己维护 memo 数组**、判\"是否算过\"、传下标。Python 的 `@lru_cache` 是**装饰器**,自动用一个 dict 缓存\"参数 → 返回值\",你只管写干净的递归。这是 Python 刷 DP 题最大的爽点。\n\n> ✅ **本章贯穿口诀**:**状态定义 + 转移方程 + 边界**。每道题先问三连:① `dp[i]`(或 `dp[i][j]`)代表什么?② 它从哪些状态转移来?③ 最小的、可以直接回答的边界值是几?\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-预览猜",
      "heading": "① 预览猜",
      "secNum": null,
      "body": "1. 爬楼梯:每次能爬 1 或 2 步,爬到第 5 阶有几种走法?(提示:f(n)=f(n-1)+f(n-2),斐波那契)\n2. 为什么裸递归算斐波那契会慢到爆炸?子问题重叠长什么样?\n3. 「最少硬币数」为什么不能贪心?(举例:面额 `[1,3,4]` 凑 6,贪心选 4+1+1=3 枚,但最优是 3+3=2 枚)\n4. 「最长递增子序列」和「最长公共子序列」差在哪?(前者一串数自己跟自己比,后者两串字符互相比)\n5. 编辑距离的三种操作(增/删/改)怎么对应到二维表的三个邻居?\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-39.2",
      "heading": "§39.2 爬楼梯:climb_stairs(LC70)🟡",
      "secNum": "39.2",
      "body": "**题面**:你每次能爬 1 或 2 步,爬到第 `n` 阶共有多少种不同走法?\n\n### 为什么这么做\n\n关键洞察——**只看最后一步**:到达第 `n` 阶,你的最后一步要么从第 `n-1` 阶爬 1 步、要么从第 `n-2` 阶爬 2 步(没有别的可能)。所以:\n\n```\nf(n) = f(n-1) + f(n-2)\nf(1) = 1   # 只能 1\nf(2) = 2   # 1+1 或 2\n```\n\n这就是**斐波那契数列**!\n\n为什么裸递归 `f(n)=f(n-1)+f(n-2)` 是 O(2ⁿ) 地狱?因为 `f(5)` 算 `f(4)+f(3)`,`f(4)` 又算 `f(3)+f(2)`——`f(3)` 被算了两次,越往下重复越多,呈指数爆炸。**子问题大量重叠** = DP 信号。\n\n### 实现:@lru_cache 记忆化递归\n\n```python\nfrom functools import lru_cache\n\ndef climb_stairs(n: int) -> int:\n    @lru_cache(None)\n    def f(k: int) -> int:\n        if k <= 2:        # 边界: f(1)=1, f(2)=2\n            return k\n        return f(k - 1) + f(k - 2)   # 转移方程\n    return f(n)\n```\n\n**亮点**:递归函数 `f` 加 `@lru_cache(None)` 后,`f(3)` 第一次算完会被缓存,之后再调直接返回——把 O(2ⁿ) 砍成 O(n)。你写的代码和裸递归**长得一模一样**,只多一行装饰器。Java 想达到同样效果,得自己开 `int[] memo`、判 `-1`、填值,啰嗦得多。\n\n> 为什么在函数内部再定义 `f` 而不是直接装饰 `climb_stairs`?——把缓存和公开 API 解耦,避免缓存被多次调用累积(可选习惯,把 `@lru_cache` 直接加到 `climb_stairs` 也对)。\n\n### 自底向上写法(对比理解)\n\n```python\ndef climb_stairs(n: int) -> int:\n    if n <= 2:\n        return n\n    dp = [0] * (n + 1)\n    dp[1], dp[2] = 1, 2\n    for i in range(3, n + 1):\n        dp[i] = dp[i - 1] + dp[i - 2]\n    return dp[n]\n```\n\n进一步还能优化空间到 O(1):只留两个变量滚动(`a, b = b, a + b`),但 DP 表更直观,面试先讲表。\n\n> 🟢 **Java 秒懂**:`dp` 数组那版几乎是 1:1 翻译(Java `int[] dp = new int[n+1];`)。\n> 🟡 **差异**:`@lru_cache` 是 Python 独有的优雅;Java 记忆化要手写 memo。\n> ⚠️ **坑**:`n<=2` 直接返回 `n`(即 `f(1)=1, f(2)=2`),别写成 `f(0)=0, f(1)=1` 再算——能对,但多绕一步容易写错边界。\n\n### 复杂度\n\n- 时间 O(n):每个子问题 `f(k)` 只算一次。\n- 空间 O(n):递归栈深度 + lru_cache 缓存 n 项;自底向上空间 O(n),滚动优化 O(1)。\n\n---",
      "exerciseFunctions": [
        "climb_stairs"
      ]
    },
    {
      "id": "sec-39.3",
      "heading": "§39.3 零钱兑换:coin_change(LC322)🟡",
      "secNum": "39.3",
      "body": "**题面**:给定硬币面额 `coins` 和金额 `amount`,凑成该金额的**最少硬币数**;凑不出返回 `-1`。每种硬币**无限个**(完全背包)。\n\n### 为什么这么做 —— 为什么不能贪心\n\n直觉是\"每次挑最大面额尽量多拿\"(贪心)。但贪心**会出错**:\n\n```\ncoins=[1,3,4], amount=6\n贪心: 4 + 1 + 1 = 3 枚   ← 错!\n最优: 3 + 3     = 2 枚   ✓\n```\n\n贪心只顾眼前,会错过\"两个 3 比 4+1+1 更优\"的组合。**必须穷举所有子问题**——这就是 DP。\n\n### 状态定义 + 转移\n\n```\ndp[i] = 凑成金额 i 所需的最少硬币数\n转移: dp[i] = min( dp[i - coin] + 1 )   for coin in coins, if coin <= i\n        ↑ 用一枚面额 coin 后,剩下的 i-coin 已经是最优,再 +1(这枚)\n边界: dp[0] = 0     (金额 0 不需要硬币)\n```\n\n**哨兵技巧**:无法凑出的金额怎么表示?把 `dp` 初始化成 `amount + 1`(一个\"不可能的大值\")——因为最优答案最多用 `amount` 个 1 元硬币,真实答案**绝不可能超过 `amount`**。所以 `amount+1` 就是\"无穷大\"的替身。最后看 `dp[amount]`,若仍 `> amount` 说明不可达,返回 `-1`。\n\n> 这是 DP 题里**最常见的哨兵模式**:用\"理论上界 +1\"代替 `float('inf')`,避免类型混乱,也方便 `min` 运算。\n\n### 实现\n\n```python\ndef coin_change(coins: list[int], amount: int) -> int:\n    MAX = amount + 1                      # 哨兵 = 不可能的大值\n    dp = [MAX] * (amount + 1)\n    dp[0] = 0                             # 边界\n    for i in range(1, amount + 1):\n        for c in coins:\n            if c <= i and dp[i - c] + 1 < dp[i]:\n                dp[i] = dp[i - c] + 1\n    return dp[amount] if dp[amount] != MAX else -1\n```\n\n**走一遍 `[1,2,5], amount=11`**:`dp[0]=0, dp[1]=1, dp[2]=1, dp[5]=1, dp[6]=2(5+1), ... dp[11]=3(10+1 / 6+5 / 9+2 的最优是 5+5+1)`。\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:`int[] dp = new int[amount+1]; Arrays.fill(dp, amount+1); dp[0]=0;` 双层 for 一模一样。\n> ⚠️ **坑**:\n> 1. 忘了 `amount=0` 返回 0(题目明确 0 元要 0 枚)。\n> 2. 哨兵写成 `float('inf')` 再 `+1` 会得到 `inf`,逻辑没错但最后比较 `> amount` 才是对的判据;新手容易直接 `return dp[amount]` 漏掉 `-1` 转换。\n> 3. 内层循环 `if c <= i` 必须判,否则 `dp[i-c]` 越界。\n\n### 复杂度\n\n- 时间 O(amount × len(coins)):两层循环。\n- 空间 O(amount):一维 dp 表。\n\n---",
      "exerciseFunctions": [
        "coin_change"
      ]
    },
    {
      "id": "sec-39.4",
      "heading": "§39.4 最长递增子序列:length_of_lis(LC300)🟡",
      "secNum": "39.4",
      "body": "**题面**:给定整数数组,返回**严格递增子序列**的最大长度(子序列**不要求连续**,但要保序)。\n\n### 为什么这么做\n\n注意两个限定:**严格递增**(`nums[j] < nums[i]`,不能等于)+ **不连续**(可跳着选,区别于\"子串/子数组\"必须连续)。\n\n经典 **一维 DP** 定义——以\"位置 i 结尾\"作状态:\n\n```\ndp[i] = 以 nums[i] 结尾的 LIS 长度\n转移: dp[i] = max( dp[j] + 1 )   for all j < i 且 nums[j] < nums[i]\n边界: dp[i] = 1   (至少包含 nums[i] 自己)\n答案: max(dp)   (LIS 可能以任意位置结尾,取全局最大)\n```\n\n**走一遍** `[10,9,2,5,3,7,101,18]`:\n\n| i | nums[i] | 能接在谁后面 (j) | dp[i] |\n|---|---------|------------------|-------|\n| 0 | 10 | (无) | 1 |\n| 1 | 9 | (无,9<前面的10不行) | 1 |\n| 2 | 2 | (无) | 1 |\n| 3 | 5 | 2(2<5) | dp[2]+1=2 |\n| 4 | 3 | 2(2<3) | dp[2]+1=2 |\n| 5 | 7 | 2,5,3 | max(1,2,2)+1=3 |\n| 6 | 101 | 2,5,3,7,9,10 | 3+1=4 |\n| 7 | 18 | 2,5,3,7,9,10 | 3+1=4 |\n\n`max(dp)=4`,答案 4(LIS 如 `[2,3,7,101]` 或 `[2,5,7,101]`)。\n\n### 实现\n\n```python\ndef length_of_lis(nums: list[int]) -> int:\n    if not nums:\n        return 0\n    n = len(nums)\n    dp = [1] * n              # 边界:每个元素自身是长度 1 的 LIS\n    for i in range(n):\n        for j in range(i):\n            if nums[j] < nums[i] and dp[j] + 1 > dp[i]:\n                dp[i] = dp[j] + 1\n    return max(dp)\n```\n\n### 进阶:O(n log n) 的 patience sorting(bisect)\n\n面试官常追问\"能不能更快\"。**贪心 + 二分**思路:维护一个数组 `tails`,`tails[k]` = 长度为 k+1 的所有递增子序列的**最小末尾**。每来一个新数,用 `bisect_left` 找它该替换/追加的位置:\n\n```python\nimport bisect\ndef length_of_lis_fast(nums):\n    tails = []\n    for x in nums:\n        i = bisect.bisect_left(tails, x)   # 第一个 >= x 的位置\n        if i == len(tails):\n            tails.append(x)                 # x 比所有都大 -> 接长\n        else:\n            tails[i] = x                    # 否则替换,让末尾更小\n    return len(tails)\n```\n\n注意:这个 `tails` **不是真实的 LIS**(只是长度对),但能求出**长度**且 O(n log n)。本章作业用 O(n²) 版本即可,进阶版面试加分。\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:双层 for + `dp` 数组,完全 1:1。\n> 🟡 **进阶差异**:Java 的二分要自己写或用 `Arrays.binarySearch`(返回负数表示插入点,要 `-i-1` 转换);Python 直接 `bisect.bisect_left`,语义清晰。\n> ⚠️ **坑**:\n> 1. 答案是 `max(dp)` 不是 `dp[-1]`——LIS 不一定以最后一个元素结尾。\n> 2. 空数组返回 0(题面隐含),忘了判 `if not nums` 会 `max([])` 报错。\n> 3. **严格递增**用 `<` 不是 `<=`;若题改成\"非递减\"才用 `<=`,别混。\n\n### 复杂度\n\n- DP 版:时间 O(n²),空间 O(n)。\n- 进阶版:时间 O(n log n),空间 O(n)。\n\n---",
      "exerciseFunctions": [
        "length_of_lis"
      ]
    },
    {
      "id": "sec-39.5",
      "heading": "§39.5 最长公共子序列:longest_common_subsequence(LC1143)🟡",
      "secNum": "39.5",
      "body": "**题面**:两个字符串 `text1`、`text2` 的**最长公共子序列**长度(可不连续,但保序)。\n\n### 为什么这么做\n\n两个串互相比——升到**二维状态**。经典定义:\n\n```\ndp[i][j] = text1 的前 i 个字符 与 text2 的前 j 个字符 的 LCS 长度\n```\n\n转移看最后那对字符 `text1[i-1]` 和 `text2[j-1]`(用 i-1 是因为字符串下标从 0,而 dp 从 1 开始留出\"空前缀\"边界):\n\n```\n若 text1[i-1] == text2[j-1]:   # 这对字符相等,可以配对收尾\n    dp[i][j] = dp[i-1][j-1] + 1\n否则:                          # 不等,各退一格取较大\n    dp[i][j] = max(dp[i-1][j], dp[i][j-1])\n边界: dp[0][*] = dp[*][0] = 0   # 空串和任何串的 LCS = 0\n答案: dp[len1][len2]\n```\n\n**直观理解转移**:两字符相等,这俩就是 LCS 的一部分,前面的 LCS 长度 +1;不等,那这俩至少有一个**不进** LCS,分别尝试扔掉一个取较大。\n\n**走一遍** `\"abcde\"` vs `\"ace\"`(dp 表 4×6,行是 abcde、列是 ace):\n\n```\n      \"\"  a   c   e\n  \"\"   0  0   0   0\n  a    0  1   1   1     # a==a -> dp[0][0]+1=1\n  b    0  1   1   1     # b不配 -> max(上,左)\n  c    0  1   2   2     # c==c -> dp[1][1]+1=2\n  d    0  1   2   2     # 不配\n  e    0  1   2   3     # e==e -> dp[4][2]+1=3\n```\n\n`dp[5][3]=3`,答案 3(即 `\"ace\"`)。\n\n### 实现\n\n```python\ndef longest_common_subsequence(text1: str, text2: str) -> int:\n    len1, len2 = len(text1), len(text2)\n    # dp 表开 (len1+1) x (len2+1),第 0 行/列全 0(空前缀边界)\n    dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]\n    for i in range(1, len1 + 1):\n        for j in range(1, len2 + 1):\n            if text1[i - 1] == text2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1] + 1\n            else:\n                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])\n    return dp[len1][len2]\n```\n\n### Java 对比 + 二维表的常见写法\n\n> 🟢 **Java 秒懂**:`int[][] dp = new int[len1+1][len2+1];` 几乎逐字翻译。\n> 🔴 **Python 特有**:**二维 list 的初始化**是个经典坑——\n> ```python\n> dp = [[0] * (len2 + 1)] * (len1 + 1)   # ❌ 错!所有行是同一个对象\n> dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]   # ✓ 每行独立\n> ```\n> `[x]*n` 对**不可变**元素(0)是安全的,但 `[[..]]*n` 会让 n 行指向**同一行**,改一个全变。**必须用列表推导式**。这是 Java 老手写 Python 二维表的第一大坑(Java 的 `new int[][]` 天然每行独立,没有这问题)。\n> ⚠️ **其他坑**:`i-1`/`j-1` 别越界(所以从 1 开始循环);别把 `dp[i-1][j-1]+1` 写成 `dp[i][j]+1`(自引用)。\n\n### 复杂度\n\n- 时间 O(len1 × len2):填满整张表。\n- 空间 O(len1 × len2);滚动数组可优化到 O(min(len1,len2))。\n\n---",
      "exerciseFunctions": [
        "longest_common_subsequence"
      ]
    },
    {
      "id": "sec-39.6",
      "heading": "§39.6 编辑距离:min_distance(LC72 Hard)🔴",
      "secNum": "39.6",
      "body": "**题面**:把 `word1` 变成 `word2`,最少需要几次**单字符操作**?操作有三种:**插入 / 删除 / 替换** 一个字符。\n\n### 为什么这么做 —— LCS 的升级版\n\n这是二维 DP 的天花板题之一。和 LCS 几乎同构,但相等时要看\"不操作\",不等时**多了替换**。状态:\n\n```\ndp[i][j] = word1 前 i 个字符 变成 word2 前 j 个字符 的最少操作数\n```\n\n转移:\n\n```\n若 word1[i-1] == word2[j-1]:   # 这对字符已相等,白嫖,不操作\n    dp[i][j] = dp[i-1][j-1]\n否则:                          # 不等,三选一取最小 +1\n    dp[i][j] = 1 + min(\n        dp[i-1][j],      # 删除 word1[i-1]    (word1 退一格)\n        dp[i][j-1],      # 插入(=在 word1 加 word2[j-1],word2 退一格)\n        dp[i-1][j-1],    # 替换 word1[i-1] 成 word2[j-1]\n    )\n边界:\n    dp[i][0] = i   # 把 word1 前 i 个全删了变空串\n    dp[0][j] = j   # 从空串插入 j 个字符变 word2 前 j 个\n```\n\n**三种操作怎么对应三个邻居**(画表秒懂):\n- `dp[i-1][j]` 是**上方** → word1 多消耗一个字符(删掉它)。\n- `dp[i][j-1] 是**左方** → word2 多匹配一个(在 word1 插入它)。\n- `dp[i-1][j-1]` 是**左上** → 两个指针都前进(替换,或相等时白嫖)。\n\n**走一遍** `\"horse\"` → `\"ros\"`(简略):最终 `dp[5][3]=3`(horse→rorse→rose→ros)。\n\n### 实现\n\n```python\ndef min_distance(word1: str, word2: str) -> int:\n    len1, len2 = len(word1), len(word2)\n    dp = [[0] * (len2 + 1) for _ in range(len1 + 1)]\n    # 边界:空串\n    for i in range(len1 + 1):\n        dp[i][0] = i\n    for j in range(len2 + 1):\n        dp[0][j] = j\n    for i in range(1, len1 + 1):\n        for j in range(1, len2 + 1):\n            if word1[i - 1] == word2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1]   # 白嫖\n            else:\n                dp[i][j] = 1 + min(\n                    dp[i - 1][j],      # 删\n                    dp[i][j - 1],      # 插\n                    dp[i - 1][j - 1],  # 替\n                )\n    return dp[len1][len2]\n```\n\n### Java 对比\n\n> 🟢 **Java 秒懂**:二维 `int[][]`,三层填表逻辑完全一致。Hard 的\"难\"在**想到状态定义**,不在语言。\n> 🟡 **差异**:同样是 `[[0]*w]*h` 的初始化坑(见 §39.5),用列表推导式。\n> ⚠️ **坑**:\n> 1. **边界别漏**:空串情况 `dp[i][0]=i`、`dp[0][j]=j` 必须显式填,否则默认 0 会把\"全删/全插\"算成 0 次。\n> 2. 字符相等时是 `dp[i-1][j-1]`(**不加 1**,白嫖),不等时才 `+1`——两者别混。\n> 3. 别忘了 `min` 里**三个**分支,漏掉\"替换\"会退化成只能增删(那是另一道题:只有增删的编辑距离 = `len1+len2-2*LCS`)。\n\n### 复杂度\n\n- 时间 O(len1 × len2):填满整张表。\n- 空间 O(len1 × len2);滚动数组优化到 O(min(len1,len2))。\n\n---",
      "exerciseFunctions": [
        "min_distance"
      ]
    },
    {
      "id": "sec-39.7",
      "heading": "§39.7 DP 通用方法论 + 原型速查(讲透不出题)",
      "secNum": "39.7",
      "body": "### 解 DP 题的四步套路(背下来)\n\n1. **定义状态**:`dp[i]` / `dp[i][j]` 代表什么?(一句话,要\"无后效\")\n2. **写转移方程**:`dp[i]` 从哪些状态来?取 `max`/`min`/`+`?\n3. **定边界 + 初始值**:最小子问题直接给答案(如 `dp[0]=0`),不可达用哨兵。\n4. **确定计算顺序 + 答案位置**:自底向上从小填大;答案在 `dp[n]` 还是 `max(dp)`?\n\n### 五大原型速查表\n\n| 原型 | 代表题 | 状态 | 转移核心 |\n|------|--------|------|----------|\n| 一维递推(斐波那契型) | 爬楼梯 §39.2 | `dp[i]`=到 i 的方案数 | `dp[i]=dp[i-1]+dp[i-2]` |\n| 背包(完全/0-1) | 零钱兑换 §39.3 | `dp[i]`=凑 i 的最少/方案 | `dp[i]=min(dp[i-c]+1)` |\n| 一维区间(LIS 类) | LIS §39.4 | `dp[i]`=以 i 结尾 | `dp[i]=max(dp[j]+1)` |\n| 双串公共(LCS 类) | LCS §39.5 | `dp[i][j]`=前 i × 前 j | 相等 +1,否则 max(上,左) |\n| 双串变换(编辑距离类) | Edit §39.6 | `dp[i][j]`=i→j 的代价 | 相等不变,否则 min(增删改)+1 |\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-39.8",
      "heading": "§39.8 Java 老手常踩的坑 ⚠️",
      "secNum": "39.8",
      "body": "1. **裸递归不记忆化**:斐波那契、爬楼梯写裸递归 → O(2ⁿ) 超时。要么 `@lru_cache`,要么填表。\n2. **二维表用 `[[0]*w]*h`**:`*` 复制是浅拷贝,所有行指向同一对象,改一个全变。**必须列表推导式** `[[0]*w for _ in range(h)]`(§39.5/§39.6 大坑)。\n3. **答案取错位置**:LIS 答案是 `max(dp)` 不是 `dp[-1]`(LIS 不一定以末尾结束);爬楼梯答案在 `dp[n]`。\n4. **哨兵漏判**:`coin_change` 不可达要返回 `-1`,别直接 `return dp[amount]`(可能是哨兵)。\n5. **边界不填**:编辑距离的 `dp[i][0]=i`、`dp[0][j]=j` 必须显式填,否则全算成 0。\n6. **严格 vs 非严格搞混**:LIS 用 `<`(严格),题改\"非递减\"才用 `<=`。\n7. **三操作漏分支**:编辑距离 `min(删,插,替)` 三个都要,漏\"替换\"答案错。\n8. **`@lru_cache` 加在错的函数上**:可变参数(list)不能 hash → 不能缓存;要缓存就把 list 转 tuple,或缓存下标版。\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-本章作业",
      "heading": "📝 本章作业",
      "secNum": null,
      "body": "| 任务 | 知识点 | 难度 |\n|------|--------|------|\n| `climb_stairs` | 一维递推 + `@lru_cache` | 🟡 |\n| `coin_change` | 完全背包求最小 + 哨兵 | 🟡 |\n| `length_of_lis` | 一维 DP O(n²) | 🟡 |\n| `longest_common_subsequence` | 二维 DP 双串 | 🟡 |\n| `min_distance` | 编辑距离 Hard | 🔴 |\n\n```bash\nuv run pytest 06_leetcode/ch39/test_ch39_assignment.py -v\n```\n\n全绿 = 你掌握了 Ch39。\n\n---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-自测",
      "heading": "✅ 自测",
      "secNum": null,
      "body": "- [ ] 能背出 DP 四步套路:状态 → 转移 → 边界 → 顺序/答案位置\n- [ ] 会用 `@lru_cache` 写记忆化,知道它怎么把 O(2ⁿ) 砍成 O(n)\n- [ ] 知道 coin_change 为什么不能贪心,会写哨兵判 `-1`\n- [ ] LIS 的 `dp[i]` 是\"以 i 结尾\"不是\"前 i 个\",答案是 `max(dp)`\n- [ ] 二维表会用 `[[0]*w for _ in range(h)]` 避免 `*h` 浅拷贝坑\n- [ ] 编辑距离三操作能对到表的\"上/左/左上\"三个邻居\n- [ ] 5 个作业全绿",
      "exerciseFunctions": []
    },
    {
      "id": "sec-费曼挑战",
      "heading": "🎓 费曼挑战",
      "secNum": null,
      "body": "1. 「爬楼梯裸递归为什么会慢到爆炸?`@lru_cache` 怎么救?Java 里你得怎么手动做这件事?」— 重读 §39.2\n2. 「零钱兑换为什么不能贪心?`amount+1` 这个哨兵为什么不影响答案正确性?」— 重读 §39.3\n3. 「LIS 的 `dp[i]` 为什么定义成'以 i 结尾'而不是'前 i 个'?两种定义答案位置分别在哪?」— 重读 §39.4\n4. 「LCS 和编辑距离的状态转移几乎一样,区别在哪一格?为什么编辑距离相等时不 +1?」— 重读 §39.5 + §39.6\n5. 「写 Python 二维 DP 表,`[[0]*w]*h` 会出什么 bug?为什么?」— 重读 §39.5",
      "exerciseFunctions": []
    },
    {
      "id": "sec-记忆闪卡-review-md-review-md",
      "heading": "🧠 记忆闪卡 → [`review.md`](./review.md)",
      "secNum": null,
      "body": "---",
      "exerciseFunctions": []
    },
    {
      "id": "sec-下一步-Ch40-回溯-贪心-综合",
      "heading": "⏭️ 下一步:Ch40 回溯 / 贪心 + 综合",
      "secNum": null,
      "body": "DP 解决\"求最值/方案数且子问题重叠\"。接下来 **回溯**(DFS 枚举所有可能:全排列、组合、N 皇后)+ **贪心**(每步局部最优 = 全局最优的特定题型)。这两类和 DP 互补:回溯是\"穷举所有方案\",贪心是\"不回头地选\",DP 是\"重叠子问题求最优\"。凑齐 M6 工具箱。",
      "exerciseFunctions": []
    }
  ],
  "functions": [
    {
      "name": "climb_stairs",
      "testClass": "TestClimbStairs",
      "skeleton": "def climb_stairs(n: int) -> int:\n    \"\"\"\n    【爬楼梯 LC70 · §39.2】每次能爬 1 或 2 步,爬到第 n 阶有几种不同走法?\n\n    示例:\n        climb_stairs(2) -> 2   # 1+1, 2\n        climb_stairs(3) -> 3   # 1+1+1, 1+2, 2+1\n        climb_stairs(5) -> 8\n        climb_stairs(1) -> 1\n\n    思路(@lru_cache 记忆化递归,f(n)=f(n-1)+f(n-2)):\n        到达第 n 阶,最后一步要么从 n-1 爬 1 步、要么从 n-2 爬 2 步,\n        所以走法 = f(n-1) + f(n-2)。这就是斐波那契,f(1)=1, f(2)=2。\n        用 @lru_cache(None) 自动缓存子问题答案,避免指数级重复计算。\n    \"\"\"\n    # TODO: 按上方「思路」实现(可用 @lru_cache 记忆化:f(n)=f(n-1)+f(n-2),f(1)=1,f(2)=2)\n    ..."
    },
    {
      "name": "coin_change",
      "testClass": "TestCoinChange",
      "skeleton": "def coin_change(coins: list[int], amount: int) -> int:\n    \"\"\"\n    【零钱兑换 LC322 · §39.3】用最少的硬币凑成 amount,凑不出返回 -1。每种硬币无限个。\n\n    示例:\n        coin_change([1,2,5], 11)  -> 3   # 5+5+1\n        coin_change([2], 3)       -> -1\n        coin_change([1], 0)       -> 0\n        coin_change([1], 2)       -> 2\n\n    思路(自底向上 DP,完全背包求最小):\n        dp[i] = 凑成金额 i 所需的最少硬币数。\n        转移:dp[i] = min(dp[i-coin] + 1)  for coin in coins if coin <= i\n        初始:dp[0] = 0(金额 0 不要硬币),其余初始化成 amount+1(表示\"无穷大/不可达\")。\n        最后 dp[amount] > amount 说明凑不出,返回 -1。\n        用 amount+1 当哨兵是因为最多用 amount 个 1 元硬币,真实答案不可能超过它。\n    \"\"\"\n    # TODO: 按上方「思路」实现(自底向上 DP,dp[i]=min(dp[i-c]+1),哨兵 amount+1)\n    ..."
    },
    {
      "name": "length_of_lis",
      "testClass": "TestLengthOfLis",
      "skeleton": "def length_of_lis(nums: list[int]) -> int:\n    \"\"\"\n    【最长递增子序列 LC300 · §39.4】返回严格递增子序列的最大长度(不要求连续)。\n\n    示例:\n        length_of_lis([10,9,2,5,3,7,101,18]) -> 4   # [2,3,7,101] 或 [2,5,7,101]\n        length_of_lis([0,1,0,3,2,3])         -> 4   # [0,1,2,3]\n        length_of_lis([7,7,7,7])             -> 1\n        length_of_lis([])                    -> 0\n\n    思路(O(n^2) DP):\n        dp[i] = 以 nums[i] 结尾的 LIS 长度。\n        对所有 j<i 且 nums[j]<nums[i]:dp[i] = max(dp[i], dp[j]+1)。\n        全程初始化 dp[i]=1(至少包含自己)。答案 = max(dp)。\n        空数组返回 0。\n    \"\"\"\n    # TODO: 按上方「思路」实现(dp[i]=以 nums[i] 结尾的 LIS,答案 max(dp);空数组 0)\n    ..."
    },
    {
      "name": "longest_common_subsequence",
      "testClass": "TestLongestCommonSubsequence",
      "skeleton": "def longest_common_subsequence(text1: str, text2: str) -> int:\n    \"\"\"\n    【最长公共子序列 LC1143 · §39.5】两个字符串的最长公共子序列长度(可不连续)。\n\n    示例:\n        longest_common_subsequence(\"abcde\", \"ace\") -> 3   # \"ace\"\n        longest_common_subsequence(\"abc\", \"abc\")   -> 3\n        longest_common_subsequence(\"abc\", \"def\")   -> 0\n\n    思路(二维 DP):\n        dp[i][j] = text1 前 i 个字符 与 text2 前 j 个字符 的 LCS 长度。\n        - 若 text1[i-1]==text2[j-1]:dp[i][j] = dp[i-1][j-1] + 1  (这对字符配对)\n        - 否则:dp[i][j] = max(dp[i-1][j], dp[i][j-1])  (扔掉一个字符取较大)\n        初始:dp[0][*]=dp[*][0]=0(空串与任何串的 LCS=0)。\n        答案 = dp[len1][len2]。\n        为了让 i-1/j-1 不越界,dp 表开 (len1+1)x(len2+1),第 0 行/列全 0。\n    \"\"\"\n    # TODO: 按上方「思路」实现(二维 DP;相等 +1,否则 max(上,左))\n    ..."
    },
    {
      "name": "min_distance",
      "testClass": "TestMinDistance",
      "skeleton": "def min_distance(word1: str, word2: str) -> int:\n    \"\"\"\n    【编辑距离 LC72 Hard · §39.6】把 word1 变成 word2 最少需要几次操作\n    (插入 / 删除 / 替换 一个字符)。\n\n    示例:\n        min_distance(\"horse\", \"ros\")     -> 3   # horse->rorse->rose->ros\n        min_distance(\"intention\", \"execution\") -> 5\n        min_distance(\"\", \"\")             -> 0\n        min_distance(\"a\", \"\")            -> 1   # 删 a\n        min_distance(\"\", \"a\")            -> 1   # 插 a\n\n    思路(二维 DP,LCS 的升级版——多了\"替换\"操作):\n        dp[i][j] = word1 前 i 个字符 变成 word2 前 j 个字符 的最少操作数。\n        - 若 word1[i-1]==word2[j-1]:dp[i][j] = dp[i-1][j-1]  (这对字符已相等,不操作)\n        - 否则取三种操作的最小值 +1:\n            dp[i-1][j]   + 1   # 删除 word1[i-1]\n            dp[i][j-1]   + 1   # 在 word1 插入一个字符(等价于删除 word2[j-1])\n            dp[i-1][j-1] + 1   # 替换 word1[i-1] 成 word2[j-1]\n        初始:\n            dp[i][0] = i  (把 word1 前 i 个字符全删了变成空串)\n            dp[0][j] = j  (从空串插入 j 个字符变成 word2 前 j 个)\n    \"\"\"\n    # TODO: 按上方「思路」实现(二维 DP;相等不+1,否则 1+min(删/插/替);边界 dp[i][0]=i,dp[0][j]=j)\n    ..."
    }
  ],
  "preamble": "\"\"\"\nCh39 作业:动态规划(Dynamic Programming)。\n\nDP 是面试核心。核心思想:**把大问题拆成子问题,记住子问题的答案避免重复计算**。\n两种写法:\n  - 自顶向下(记忆化搜索):写递归 + 用缓存记住已算的子问题。Python 用 @lru_cache\n    写得极优雅;Java 要手写数组/HashMap 当缓存。\n  - 自底向上(迭代 DP):从最小子问题开始填表,用 list 当 DP 表。\n\n本章 5 道经典题,覆盖 DP 三大原型:\n  1. 一维递推(爬楼梯:斐波那契型)\n  2. 背包/凑硬币(完全背包求最少硬币)\n  3. 子序列(LIS、LCS)\n  4. 区间/双串(编辑距离)\n\n纯 stdlib。在每处 TODO 写实现,然后:\n\n    uv run pytest 06_leetcode/ch39/test_ch39_assignment.py -v\n\n全绿 = 你掌握了 Ch39。\n\"\"\"\nfrom functools import lru_cache\n\n\n# ========== §39.2 爬楼梯:climb_stairs ==========\n\n\n# ========== §39.3 零钱兑换:coin_change ==========\n\n\n# ========== §39.4 最长递增子序列:length_of_lis ==========\n\n\n# ========== §39.5 最长公共子序列:longest_common_subsequence ==========\n\n\n# ========== §39.6 编辑距离:min_distance ==========\n\n\n# ---------------------------------------------------------------------\nif __name__ == \"__main__\":\n    print(\"climb_stairs(5) =\", climb_stairs(5))  # 8\n    print(\"coin_change([1,2,5], 11) =\", coin_change([1, 2, 5], 11))  # 3\n    print(\"length_of_lis([10,9,2,5,3,7,101,18]) =\", length_of_lis([10, 9, 2, 5, 3, 7, 101, 18]))  # 4\n    print(\"longest_common_subsequence('abcde','ace') =\", longest_common_subsequence(\"abcde\", \"ace\"))  # 3\n    print(\"min_distance('horse','ros') =\", min_distance(\"horse\", \"ros\"))  # 3"
}